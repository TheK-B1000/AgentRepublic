# ══════════════════════════════════════════════════════════════
# Agent Republic — Environment Variables (TEMPLATE — NO SECRETS)
# ══════════════════════════════════════════════════════════════

# LLM Provider
LLM_PROVIDER=mock                    # mock | openai | gemini | claude
LLM_API_KEY=your-api-key-here
LLM_MODEL=gpt-4o                    # Model identifier

# Trace Pipeline
TRACE_STORAGE_PATH=./traces          # Local trace output directory
TRACE_FORMAT=jsonl                   # jsonl | json

# Agent Defaults
DEFAULT_MAX_STEPS=25                 # Max steps before escalation
DEFAULT_TOOL_TIMEOUT_MS=15000        # Per-tool timeout
DEFAULT_COST_BUDGET_USD=2.00         # Per-run cost cap
DEFAULT_SCRATCHPAD_MAX_TOKENS=8000   # Working memory limit

# Browser / Antigravity
BROWSER_HEADLESS=true                # true | false
MAX_CONCURRENT_SESSIONS=10           # Per-district limit
SESSION_TIMEOUT_MS=300000            # 5 minutes
BROWSER_RATE_LIMIT_PER_MIN=60        # Max nav actions/min

# Deployment
DEPLOY_TARGET=staging                # staging | production
CANARY_PERCENT=10                    # Initial canary traffic %
CANARY_DURATION_MIN=60               # Monitoring window

# Secrets Manager (injected at deploy time)
# VAULT_ADDR=https://vault.example.com
# VAULT_TOKEN=<injected-by-ci>
